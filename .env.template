# Shared secrets, sourced by opencli/os_install.sh and by
# openadmin/run-tests.sh + openpanel/run-tests.sh (to push results).
#
# Copy this to /root/playwright-test/.env and fill in real values.
# This file is gitignored (*.env) -- never commit the real one.

# GitHub personal access token with write access to
# stefanpejcic/OpenPanel (used by os_install.sh to post OS test results)
# and stefanpejcic/openpanel-tests (used by run-tests.sh to push README
# results).
GITHUB_TOKEN=

SSH_PUBLIC_KEY=
SSH_PRIVATE_FILE=
SSH_PRIVATE_KEY=

VIRTUALIZOR_API=
API_KEY=
API_HASH=
VPSID=
VPS_IP=
TEST_PASS=

DOCKER_HUB_TOKEN=
DISCORD_WEBHOOK=
